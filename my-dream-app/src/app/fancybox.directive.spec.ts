import { FancyboxDirective } from './fancybox.directive';

describe('FancyboxDirective', () => {
  it('should create an instance', () => {
    const directive = new FancyboxDirective();
    expect(directive).toBeTruthy();
  });
});
